# Conventions

## Conventional Commits

Format: `type(scope): description`

- `feat`, `fix`, `refactor`, `docs`, `chore`, `test`, `ci`
- ワークスペース固有の慣例がある場合はそちらを優先

## Persist Context

利用可能なメモリ/永続化ツールを使ってユーザーコンテキストを保存する。繰り返しの再説明を防ぐ。
